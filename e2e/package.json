{
  "name": "browseshield-e2e",
  "private": true,
  "type": "module",
  "description": "End-to-end tests for BrowseShield extension → analysis → extension flow",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report reports/html",
    "e2e": "bash run.sh"
  },
  "dependencies": {
    "@playwright/test": "^1.50.0",
    "js-yaml": "^4.1.0"
  }
}
